 What is Linear Search Algorithm ?
 
- Linear Search algorithm is a very simple search algorithm. In this type of search, a sequential search
  is made over all items one by one.  
  
- Every items is checked and if a match found then that particular item is returned otherwise the search
  continues till the end of the data collection.
